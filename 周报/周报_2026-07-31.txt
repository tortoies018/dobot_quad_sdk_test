========================================
                  周  报
========================================
姓名：________        部门：________
日期：2026-07-31      周期：2026-07-27 至 2026-07-31

----------------------------------------
一、本周完成工作
----------------------------------------
1. 环境搭建
   - 完成机器狗开发环境搭建：安装 gRPC、Eclipse Cyclone DDS、
     DDS 中间件（0.23.3）及全部 Python 依赖（PySide6、pyqtgraph 等）
   - 修复高层 Python 包安装问题，配置 DDS 网络与环境变量
   - 初始化工作区 Git 仓库

2. 通信调试
   - 梳理 gRPC（高层控制）与 Cyclone DDS（底层控制）通信原理
   - 解决 DDS 收不到数据问题（确认需使用 YAML 配置初始化中间件）

3. 程序开发（6 个 Qt 应用）
   - 四相机实时显示程序（quad_camera_app）
   - 高级状态监控（high_level_monitor）：状态/位姿/关节数据 +
     3D 轨迹 + 四相机画面
   - 机器人指令中心（command_center）：命令按钮 + 指令日志 +
     3D IMU 轨迹 + 相机画面
   - 相机标定程序（棋盘格 + AprilTag 两种方案，前后相机独立标定）
   - AprilTag 追踪定位：PnP 位姿解算、3D 轨迹绘制、
     视频录制与逐帧回放

4. 文档输出
   - 完成 Python API 参考文档

----------------------------------------
二、下周工作计划
----------------------------------------
1. 使用标定结果优化 AprilTag 追踪精度，完成运动轨迹闭环测试
2. 集成自主导航 / 避障控制逻辑到指令中心
3. 基于四相机画面开发视觉识别应用（目标检测、跟随）
4. 整理环境部署脚本，输出完整部署文档

----------------------------------------
三、需协调与帮助
----------------------------------------
1. 需确认 DDS 中间件版本（PC 端 0.23.3）与机器人固件版本是否
   完全匹配，必要时请提供匹配版本
2. 需确认相机标定结果精度是否满足后续定位需求，
   必要时请提供标定板标准参数
3. 机器人 gRPC 服务（192.168.1.6:50051）偶发无法连接，
   需确认主控程序运行状态

----------------------------------------
四、图片
----------------------------------------
（此处插入工作截图，如：四相机画面、3D 轨迹、标定结果等）

----------------------------------------
五、附件
----------------------------------------
1. Python API 参考文档
2. 标定结果 JSON（前后相机）
3. 各程序源码（quad_camera_app / high_level_monitor /
   command_center / camera_calib* / apriltag_tracker）

----------------------------------------
六、备注
----------------------------------------
- 程序运行需同时连接：WiFi（192.168.1.6，gRPC 高层）+ 有线
  （192.168.5.xxx/24，DDS 底层）
- DDS 环境变量：CYCLONEDDS_URI 指向 cyclonedds.xml
- 标定板打印时需保持原始尺寸，勿缩放
